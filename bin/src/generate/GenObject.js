class GenObject {
    /**
     * 
     * @param {object} itemObject 
     */
    constructor(itemObject) {
        this.baseScenario = {};
        this.generators = {};
    }
    if (itemObject.hasOwnProperty("")) {
        
    }
}